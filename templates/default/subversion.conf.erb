<Location /svn>

  DAV svn

  SVNPath /srv/svn/repo

  AuthType Basic

  AuthName "Subversion repository"

  AuthUserFile <%= node['subversion']['repo_dir'] %>/htpasswd

  Require valid-user

</Location>
